# Handoff — 2026-08-19

Session work (mostly done 2026-08-18) is committed on `automation-dev`. Nothing has been pushed — the branch is **10 commits ahead of `origin/automation-dev`** and needs an explicit decision to push.

## What's committed (this session's 4 commits, newest first)

```
d5f4e2d Add native Word ribbon add-in, app icon, and local landing page
2428011 Fix Pure DOCX fidelity: SDT/hyperlink flattening, run formatting, TOC field position
d7473ac Add letter-by-letter typing and licence-free Pure DOCX live preview
74653f5 Add Lekta Repair Contract v1 integration (repair-poc)
```

Below those are 6 commits from an **autonomous harness** that ran unattended during the session (author `WordReplica Automation Bootstrap <wordreplica-automation@users.noreply.github.com>`, `4037416` .. `4d58ada`). They were already in the working tree before this session's commits were made — see "Autonomous harness" below.

Working tree is clean except one intentionally-untracked file: `_golden2_run.log` (a stray run log — logs/reports are never committed per repo convention).

## 1. Lekta Repair Contract v1 — status

Implements Lekta's signed Repair Contract v1 protocol end-to-end:
- `src/word_replica/repair_contract/` — schema parsing, ES256-P1363 signature verification over RFC 8785 canonical JSON, package/binding validation. All fail-closed: unverified never counts as a pass.
- `src/word_replica/services/repair_package.py` — orchestrates a repair run against either the Word or Pure DOCX renderer (`--renderer word|pure-docx` on the CLI), so the flow works even with an unactivated Word licence.
- Real (non-stubbed) `detect_open_and_repair` / `check_fields_update_equality` detectors in `qa/word_render.py` — live-verified to work via Word COM even when Word itself is unactivated, since they only open/read, never edit.
- `word_replica.cli` — `repair-poc` / `repair-poc-resume` subcommands.
- `scripts/run_lekta_poc.py` + `RUN_LEKTA_POC.ps1` — foreground harness with preflight gates (branch must be `automation-dev`, working tree must be clean except plan/spec docs, all four package files must exist). Diagnostics are written outside the repo, under `C:\WordReplica-Automation\diagnostics\`, and the user's DOCX is never copied into git.

**Note on the plan document**: earlier in the session I referred to a plan file at `docs/superpowers/plans/2026-08-18-lekta-target-docx-local-poc.md`. That file does **not** actually exist on disk — the task breakdown (Tasks 1-8) only ever existed in conversation, not as a persisted file. `docs/superpowers/specs/2026-08-18-fidelity-contract-spec.md` exists and is related background (the G0-G9/L0-L4 fidelity contract) but is not the Lekta task plan. If a written plan is wanted going forward, it needs to be (re)created.

**Remaining blocker — Task 8** (the real Kalogjera acceptance run): needs
1. A real *signed* Lekta contract for the actual target document (all tests so far use the fixture key/contract in `tests/fixtures/repair_contract_v1/`), and
2. Two consecutive `FULL_PASS` harness runs on that real package before the Lekta-specific work itself can be committed as "accepted" (this is a harness-level acceptance gate, separate from today's code commits, which are already in).

Also note: `RepairPackageRequest`/`load_and_validate_package` trusts the **target** file's identity by filename match only, not a cryptographic hash — Lekta's contract doesn't sign a target hash. This is a known, accepted design gap, not a bug (see `package.py` docstring/comments).

## 2. Letter-by-letter typing + Pure DOCX live preview

- `InteractiveOptions.letter_by_letter` / desktop "Slovo po slovo" checkbox — paces Interactive Reconstruction character-by-character.
- `TextPreviewPlayer` (`interactive/text_preview.py`) — replays the same reconstruction blueprint into the desktop UI as a visual typing preview with **no Word dependency at all**. This is the licence-free alternative for users whose Word isn't activated; both it and real-Word interactive reconstruction are exposed as user-selectable options (`renderer` combo: "Microsoft Word" / "Pure DOCX" when Interactive Reconstruction mode is selected).

## 3. Pure DOCX / parser fidelity fixes

- Parser was silently dropping content inside `<w:sdt>` content controls (e.g. an auto-generated TOC) and not fully unwrapping `<w:hyperlink>`-wrapped runs — fixed in `parser/parser.py`.
- `pure_docx.py`'s `_run_element` only ever wrote bold/italic/underline/hidden — font name/color/size/strike/highlight/vertAlign/language/spacing were silently dropped. Fixed; verified 27→0 L2 mismatches on a real test document.
- TOC field tokens were appended to the end of the document instead of their original inline position — fixed.

## 4. Word ribbon add-in, icon, landing page

- `scripts/word_ribbon/build_ribbon_addin.py` builds a `.dotm` with a native ribbon tab (Renderer/Visibility/Fidelity/Metadata dropdowns) so reconstruction can be launched from inside Word 2010 (VBA + Custom UI — Word 2010 predates Office.js). Launch is non-blocking (`Shell cmd, vbNormalFocus`, not a synchronous `WScript.Shell.Run(...,True)` — the earlier version froze Word's UI thread).
- App icon (`assets/icon.ico`) wired into `BUILD_WINDOWS_APP.ps1` / installer scripts.
- `landing/index.html` — local only, **not deployed** (Netlify/GitHub Pages explicitly declined by the user), and the built `.exe` has not been published anywhere yet.

## Autonomous harness — what it did while unattended

5-6 commits landed from the "WordReplica Automation Bootstrap" identity during this session (golden-pipeline bug fixes: bookmark-name sanitization for Word-unsafe characters, Windows file-lock retry on rename during interactive finalization, retry window widened to 30s; plus a larger commit `4037416` adding environment-fingerprint capture, Compatibility Mode recording, and multi-golden-document support).

Notably, `4037416` did the **same `scripts/codex_automation/audit.py` → `src/word_replica/qa/golden_audit.py` extraction** that this session's own Task 4 work also did, independently and concurrently, in the same working tree. Checked carefully: no actual conflict — by the time this session's commits were made, the working tree already matched the autonomous commit's version exactly (zero diff), so nothing was lost or overwritten. This was pure coincidence of two agents solving the same well-scoped extraction the same way, not a merge issue. No action needed, but worth knowing this harness can run unattended and touch the same files a foreground session is using.

**No autonomous process is currently running** (verified via `Get-Process`/`Win32_Process` before committing — the working tree was safe to commit).

## Open issue: `golden_2` ("Kasalo diplomski") stuck at all-gates-FAIL

Discovered via a stray `_golden2_run.log` (not committed): the autonomous harness's `golden_2` target — `Kasalo - diplomski (finalno).docx` — is failing **every** gate (G0-G9) with `AUTOMATION STOP REQUIRED: three consecutive iterations without gate improvement`.

Looked at the latest report (`C:\WordReplica-Automation\diagnostics\golden_2\20260818T222356Z_4d58adac\golden_report.json`): every gate reports `"not evaluable: output hash mismatch"`, `first_divergent_gate: "G0"`, but `source_unchanged: true` and both the static and interactive subprocess exit codes are `0` — i.e. reconstruction runs without crashing, but the output doesn't hash-match at all, before any gate can even evaluate a real diff. This is a different failure shape than the earlier golden_1 issues (which were real structural mismatches on individual gates). Not yet root-caused — worth checking first whether the interactive run is actually producing a valid `.docx` at all for this document, or silently producing something empty/corrupt. Diagnostics directory: `C:\WordReplica-Automation\diagnostics\golden_2\` (gitignored, stays local).

## Known pre-existing, unrelated test failure

`tests/unit/test_persistent_harness_bootstrap.py::test_bootstrap_generates_valid_remote_source_manifest` fails on this machine — missing PIL binary wheels (`_avif`, `_imagingmorph`, etc.) in a venv snapshot used by the remote-harness bootstrap test. Confirmed present before and unrelated to any change made this session (full suite: 600 passed / 1 unrelated failure / 45 deselected `-m "not word"`).

## Suggested next steps

1. Decide whether/when to `git push` the 10 local commits to `origin/automation-dev`.
2. Investigate the `golden_2` all-gates-FAIL state (see above) — likely start by opening the interactive run's output `.docx` directly to see whether it's even valid.
3. For Lekta Task 8: get a real signed contract for the actual Kalogjera target document, then run `RUN_LEKTA_POC.ps1` for two consecutive `FULL_PASS` results.
4. If a persisted task-plan doc is wanted for the Lekta work going forward, (re)create it under `docs/superpowers/plans/` — none currently exists despite earlier references to one.
